spring:
  application:
    name: ead-notification-service
  output:
    ansi:
      enabled: Always
  config:
    import: 'optional:configserver:'
  cloud:
    config:
      discovery:
        serviceId: ead-config-server
